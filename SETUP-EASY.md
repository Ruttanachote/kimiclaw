# AI DevStudio - One-Click Setup (No PowerShell!)

## 🚀 วิธีติดตั้ง (ง่ายที่สุด)

### ขั้นตอนที่ 1: ดาวน์โหลด
- แตกไฟล์ `AI-DevStudio.zip` ไปที่ `C:\AI-DevStudio`

### ขั้นตอนที่ 2: ดับเบิลคลิก
```
📁 AI-DevStudio
   └── 📄 setup.bat  ← ดับเบิลคลิกอันนี้
```

### ขั้นตอนที่ 3: รอ
- สคริปต์จะติดตั้งทุกอย่างให้อัตโนมัติ
- รอ 3-5 นาที

### ขั้นตอนที่ 4: ใช้งาน
- เปิด browser ไปที่ `http://localhost`
- เสร็จ!

---

## 📋 สิ่งที่ต้องมีก่อน

| โปรแกรม | ถ้าไม่มี | จะเกิดอะไร |
|:---|:---|:---|
| Windows 10/11 | - | ใช้ไม่ได้ |
| Docker Desktop | สคริปต์จะเปิดหน้าโหลดให้ | ต้องลงเอง |
| Node.js | สคริปต์จะลงให้อัตโนมัติ | ไม่ต้องทำอะไร |
| API Keys | สคริปต์จะเปิด notepad ให้ใส่ | ต้องไปเอามา |

---

## 🎯 ใช้งานยังไงต่อ

### เริ่มต้น (ครั้งถัดไป)
```
ดับเบิลคลิก: start.bat
```

### หยุด
```
ดับเบิลคลิก: stop.bat
```

---

## 🔧 ถ้ามีปัญหา

### "Docker not found"
1. ไปที่ https://docker.com/products/docker-desktop
2. ดาวน์โหลด + ติดตั้ง
3. รอ Docker สตาร์ท (มีไอคอน whale ที่ taskbar)
4. รัน `setup.bat` อีกครั้ง

### "API Key ไม่ทำงาน"
1. แก้ไขไฟล์ `.env`
2. ใส่ `ANTHROPIC_API_KEY=sk-ant-...`
3. รัน `stop.bat` แล้ว `start.bat`

### "Port ถูกใช้งาน"
```
# ปิดโปรแกรมที่ใช้ port 80, 8080, 5678
# หรือแก้ไข docker-compose.yml เปลี่ยน port
```

---

## 💡 คำสั่งที่ใช้บ่อย

| ต้องการ | ทำยังไง |
|:---|:---|
| เริ่มระบบ | ดับเบิลคลิก `start.bat` |
| หยุดระบบ | ดับเบิลคลิก `stop.bat` |
| ดู logs | `docker compose logs` |
| รีสตาร์ท | `stop.bat` แล้ว `start.bat` |
| ล้างทั้งหมด | `docker compose down -v` |

---

## 🎉 เสร็จแล้วใช้งานยังไง

1. เปิด browser → `http://localhost`
2. Login (ถ้าถาม): `admin` / `admin123`
3. เห็น Dashboard → พร้อมใช้!

---

**ไม่ต้องใช้ PowerShell ไม่ต้องพิมพ์คำสั่ง ไม่ต้องรู้โค้ด**

แค่ดับเบิลคลิก 🖱️
